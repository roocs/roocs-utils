name: roocs_utils
channels:
 - conda-forge
 - defaults
dependencies:
 - aiohttp
 - cf_xarray>=0.3.1 # Note that conda-forge package is cf_xarray (!= cf-xarray)
 - cftime
 - dask>=2.6
 - filelock
 - fsspec
 - jinja2
 - loguru
 - netCDF4>=1.4
 - numpy>=1.16
 - pooch
 - python-dateutil>=2.8.1
 - xarray>=0.15
 - zarr
 - zstandard
